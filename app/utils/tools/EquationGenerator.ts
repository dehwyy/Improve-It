import RandomSimple from "@/app/utils/tools/RandomSimple";
enum Diffs {
    Easy,
    Medium,
    Hard,
    Impossible
}
class EquationGenerator {
    EasyEquation() {

    }
}

export default function* getEquations(diff = 0, count = 1): Generator<unknown, string, string> {
    return ""
}
// import { test, expect } from '@playwright/test'
//
// test.beforeEach(async ({ page }) => {
//   await page.goto('/leaderboard')
// })
//
// test('leaderboard: appearance', async ({ page }) => {
//   const pageHeading = page.getByTestId('leaderboard-heading')
//   await expect(pageHeading).toBeVisible()
//   await expect(pageHeading).toHaveClass(/text-white/)
//   const userBlock = page.getByTestId('leaderboard-item')
//   await expect(userBlock).toHaveCount(5)
//   await expect(userBlock.nth(0)).toHaveClass(/text-blue-500/)
//   await expect(userBlock.nth(0)).toHaveClass(/hover:text-red-500/)
// })

// env issues since 22.03
// fix soon

export {}
